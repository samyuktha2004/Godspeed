name: Keep HF Space Alive
on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping space
        run: curl -sf ${{ secrets.HF_SPACE_URL }}/health || true
